<?php
        if( !($db = mysql_connect('localhost', 'root', '0HFYad47')))
            die('Failed to connect to Server' - #'.mysql_errno().': '.mysql_error();
        if( !mysql_select_db( 'mh_data' ) )
            die( 'Connected to Server, but Failed to Connect to Database - #'.mysql_errno().': '.mysql_error();

        >
